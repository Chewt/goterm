Bugs I have encountered while playing:
- with mouse mode enabled, repeating commands with an empty command becomes a problem since a single mouse click return just an empty space. Maybe introduce a highlight command to highlight a spot on the board.
